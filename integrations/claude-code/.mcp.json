{
  "mcpServers": {
    "decision-memory": {
      "command": "npx",
      "args": ["-y", "@decision-memory/mcp-server@latest"],
      "env": {
        "DECISION_MEMORY_FILE": "DECISIONS.toon"
      }
    }
  }
}
